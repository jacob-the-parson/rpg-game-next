// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { CharacterAppearance } from "./character_appearance_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `character_appearance`.
 *
 * Obtain a handle from the [`characterAppearance`] property on [`RemoteTables`],
 * like `ctx.db.characterAppearance`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.characterAppearance.on_insert(...)`.
 */
export class CharacterAppearanceTableHandle {
  tableCache: TableCache<CharacterAppearance>;

  constructor(tableCache: TableCache<CharacterAppearance>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<CharacterAppearance> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `character_id` unique index on the table `character_appearance`,
   * which allows point queries on the field of the same name
   * via the [`CharacterAppearanceCharacterIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.characterAppearance.character_id().find(...)`.
   *
   * Get a handle on the `character_id` unique index on the table `character_appearance`.
   */
  character_id = {
    // Find the subscribed row whose `character_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): CharacterAppearance | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.character_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: CharacterAppearance) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: CharacterAppearance) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: CharacterAppearance) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: CharacterAppearance) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: CharacterAppearance, newRow: CharacterAppearance) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: CharacterAppearance, newRow: CharacterAppearance) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
