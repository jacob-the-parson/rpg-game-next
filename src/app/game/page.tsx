import GameEnvironment from '@/components/GameEnvironment';

export default function GamePage() {
  // Simple server component that renders the client-side game environment
  return <GameEnvironment />;
} 